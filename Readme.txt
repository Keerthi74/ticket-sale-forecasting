The following libraries are used for the project : NumpyPandasSparkML libstatsmodelpylabmatplotlibsklearnTo install any of these libraries, you can simply execute the following command: ‘!pip install Library_name’. Make sure to replace the library name with the actual library name.The dataset is already included in the zip file. upload it in the dbfs and change the first line of the code.